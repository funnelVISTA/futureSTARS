node_modules/
.DS_Store
*.log
.vercel

# Unused, and faint repeating marks in it look like an unlicensed stock
# watermark (see README). Kept locally for review, not published in a public
# repo. Nothing in the build references it — safe to delete outright.
assets/img/home-about.png
